<?xml version="1.0" encoding="UTF-8"?>
<application xmlns="http://appconfig.aura.nav.no" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://appconfig.aura.nav.no http://maven.adeo.no/xsd/appconfig.xsd">

    <name>modiasyforest</name>

    <selftest>
        <path>modiasyforest/internal/selftest</path>
        <humanReadablePath>modiasyforest/internal/selftest</humanReadablePath>
    </selftest>

    <artifacts>
        <ear groupId="no.nav.sbl.dialogarena" artifactId="modia-syforest-jee"/>
    </artifacts>

    <security serviceUserResourceAlias="srvmodiasyforest" />

    <resources>
        <rest alias="oidc-login-api" mapToProperty="oidc-redirect"/>

        <applicationProperties alias="modiasyforest.properties"/>
        <applicationProperties alias="syfotoggles.properties"/>
        <webservice alias="Sykepengesoeknad_v1" mapToProperty="sykepengesoeknad.endpoint"/>
        <webservice alias="Aktoer_v2" mapToProperty="aktoer.endpoint"/>
        <webservice alias="Sykefravaersoppfoelging_v1" mapToProperty="sykefravaersoppfoelging.endpoint"/>
        <webservice alias="Sykmelding_v1" mapToProperty="sykmelding.endpoint"/>
        <webservice alias="virksomhet:Organisasjon_v4" mapToProperty="organisasjon.endpoint"/>
        <webservice alias="virksomhet:Brukerprofil_v3" mapToProperty="brukerprofilv3.endpoint"/>
        <webservice alias="virksomhet:Arbeidsforhold_v3" mapToProperty="arbeidsforhold.endpoint"/>
        <webservice alias="virksomhet:DigitalKontakinformasjon_v1" mapToProperty="dkif.endpoint"/>

        <ldap alias="ldap" mapToProperty="ldap"/>
        <applicationCertificate alias="srvmodiasyforest"/>

        <fileLibrary alias="syfo.ledetekster.v1" mapToProperty="folder.ledetekster" />

        <baseUrl alias="isso-host"/>
        <baseUrl alias="isso-jwks"/>
        <baseUrl alias="isso-issuer"/>
        <baseUrl alias="isso.isalive"/>
        <credential alias="OpenIdConnectAgent"/>
    </resources>

    <exposed-services>
        <exposedRest path="/modiasyforest/rest" name="modiasyfoapi" description="Sykefravaer FSS REST-API"/>
    </exposed-services>

    <loadBalancer isAlive="/modiasyforest/internal/isAlive" />

</application>
